# managed-by: clawtool
# Generated by `clawtool recipe apply prettier`.
# Edits are safe — re-running the recipe refuses to overwrite an
# unmanaged file.

# build outputs
dist/
build/
out/
.next/
.nuxt/

# dependency lockfiles (Prettier reformats them inconsistently with
# what the package manager writes back on install)
package-lock.json
yarn.lock
pnpm-lock.yaml
bun.lockb

# coverage + caches
coverage/
.nyc_output/
.cache/
.parcel-cache/

# vendored deps + go.sum (Prettier doesn't know Go anyway)
vendor/

# binary + media (Prettier doesn't need to touch these)
*.png
*.jpg
*.jpeg
*.gif
*.svg
*.ico
*.pdf
*.zip
*.tar.gz
