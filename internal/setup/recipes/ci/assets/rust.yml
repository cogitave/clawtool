# managed-by: clawtool
# Generated by `clawtool recipe apply gh-actions-test` (Rust).
# Edits are safe — re-running the recipe refuses to overwrite an
# unmanaged file.

name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --all
